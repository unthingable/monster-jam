package com.github.unthingable.jam

import com.github.unthingable.jam.layer.{
  Aux,
  ClipMatrix,
  Control,
  Dpad,
  Level,
  MacroL,
  SceneL,
  Shift,
  StepSequencer,
  TrackL,
  TransportL
}

package object layer {
  // trait AllModes extends Aux with TransportL with Level with Dpad with TrackL
  // with ClipMatrix with Shift with Control with MacroL with SceneL
  // with StepSequencer
}
